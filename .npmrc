# Publish/consume @qnsc-vn packages via GitHub Packages.
# Workspace-internal @qnsc-vn/* packages still resolve locally via the pnpm
# workspace; this scope registry only applies to published resolution.
@qnsc-vn:registry=https://npm.pkg.github.com
link-workspace-packages=true
prefer-workspace-packages=true
