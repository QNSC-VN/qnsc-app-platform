# Publish/consume @qnsc packages via GitHub Packages.
# Workspace-internal @qnsc/* packages still resolve locally via the pnpm
# workspace; this scope registry only applies to published resolution.
@qnsc:registry=https://npm.pkg.github.com
link-workspace-packages=true
prefer-workspace-packages=true
