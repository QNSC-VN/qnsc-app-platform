/**
 * `@qnsc/platform-http`
 *
 * Shared Fastify/NestJS HTTP bootstrap for QNSC product backends: CORS policy,
 * cookie configuration, security headers, standard error codes, and OTel wiring.
 *
 * The concrete bootstrap helpers are extracted from the product repos in
 * **Phase 2** of the Identity Platform Migration Plan. This Phase 1 skeleton
 * exists to establish the publishable package and its release pipeline.
 */
export const PACKAGE_NAME = '@qnsc/platform-http';
